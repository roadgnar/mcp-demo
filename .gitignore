# Claude Code personal settings (auto-gitignored by Claude, but be explicit)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Results output
results/*

# Secrets — API keys (generated configs contain baked-in keys)
.env.local
.mcp.json
.cursor/mcp.json
